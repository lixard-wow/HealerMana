# CHANGELOG_DEV

## Unreleased

### Format
- PROMPT X
  - Intent:
  - Files changed:
  - Result:
  - Notes:

### Entries
- PROMPT 66
  - Intent: Confirmed the "healer dies but tracker doesn't show Dead" bug (PROMPT 63/64) fixed in real play. User asked for a clean/simplify/dead-code pass across the whole addon and a version bump, ahead of eventually publishing to CurseForge (project ID and release pipeline wired up separately this session).
  - Files changed: Core.lua, HealerMana.lua, UI.lua, Commands.lua, Config.lua, HealerMana.toc, CHANGELOG.md, docs/TODO.md
  - Result: Removed the now-confirmed-fixed TEMP dead-state auto-detector (HM.autoDetectDeadMismatch, reportedDeadMismatch, and its 1s ticker) — the real fix (UNIT_CONNECTION tracking) stays. Left the Innervate/Food&Drink /hm regendebug and /hm raidregen TEMP commands alone since that detection is still unconfirmed/WIP. Deduplicated the MANA/issecretvalue/C_StringUtil.WrapString/CurveConstants.ScaleTo100 module-local detection that Core.lua, UI.lua, and Commands.lua were each recomputing independently — now computed once in Core.lua and exposed on HM.*, with each file keeping its own local alias for perf. Extracted UI.lua's duplicated bar/icon pct-text formatting into one local setPctText(fs, pct). Extracted Commands.lua's duplicated raid/party unit-enumeration (used by both /hm debug and /hm regendebug's fallback scans) into one local forEachGroupUnit(fn). Removed the unused ADDON_NAME local from Core.lua, UI.lua, Config.lua, and Commands.lua (none of them actually used it). No comments existed anywhere already. Bumped HealerMana.toc to 1.0.1 and retitled CHANGELOG.md's Unreleased section to 1.0.1 (same already-listed color-customization content, not a new release yet).
  - Notes: All changes are structural dedup/cleanup only — no rendering, event-handling, or WIP feature behavior was changed. Ran luacheck via the wow-addon skill — 0 new warnings. Separately, user found a live bug (Icon Tint Opacity slider past ~90 in test mode makes fake healers vanish and not self-recover) — logged to docs/TODO.md, not fixed here since there's no confirmed root cause yet (no visible Lua error without Show Lua Errors/BugSack enabled).
- PROMPT 65
  - Intent: User asked whether click-to-battle-rez (click a dead healer's bar/icon to cast the clicking player's own brez on them, regardless of class) is possible. Researched feasibility and a simple approach; user then asked to defer implementation and save the findings for later rather than build now.
  - Files changed: docs/TODO.md
  - Result: Confirmed feasible. Verified current battle-rez classes/spells (Druid Rebirth, Death Knight Raise Ally, Paladin Intercession — Warlock Soulstone and Hunter excluded, neither is a true mid-combat corpse-rez) and that combat-res spells are explicitly whitelisted from WoW's secret-value restrictions. Landed on a simple approach avoiding any Lua class-detection: a static 3-line macro on a SecureActionButtonTemplate, letting unknown spells silently no-op. Real, unavoidable constraint: secure-button unit targeting can only be set outside combat, same limitation every click-cast raid addon has. No code changed — full research, approach, and one open design question (click vs. Shift+Click) written to docs/TODO.md's Backlog so a future session can resume without re-researching.
  - Notes: Deferred at user's explicit request, not abandoned — "save it for maybe later use." No luacheck run since no .lua files changed.
- PROMPT 64
  - Intent: While waiting for the auto-detector (PROMPT 63) to catch a real occurrence, user asked to compare against other addons that correctly display dead status (ElvUI) to find the actual gap, rather than keep waiting blind.
  - Files changed: Core.lua, HealerMana.lua
  - Result: Read ElvUI's actual installed source (real, currently-running code on this machine, not a GitHub mirror) — its health-status tag system triggers on 'UNIT_HEALTH UNIT_MAXHEALTH UNIT_CONNECTION PARTY_MEMBER_ENABLE PARTY_MEMBER_DISABLE', more events than HealerMana's current UNIT_FLAGS/UNIT_HEALTH/UNIT_MAXHEALTH/UNIT_AURA. Verified each candidate against Blizzard's own live default-UI source (Gethe/wow-ui-source) rather than assume ElvUI's list was meaningful as-is: PARTY_MEMBER_ENABLE/DISABLE turned out to be a red herring — Blizzard's own CompactUnitFrame.lua registers them plain (not per-unit) and only for power-bar refresh, unrelated to death. But UNIT_CONNECTION was confirmed as the real gap: Blizzard's own CompactUnitFrame_UpdateStatusText (triggered by UNIT_CONNECTION, registered via RegisterUnitEvent) explicitly checks both UnitIsConnected and UnitIsDeadOrGhost to decide the Dead/offline status text on their default raid frames — exactly the code path HealerMana is missing a trigger for. Added UNIT_CONNECTION to Core.lua's per-unit RegisterUnitEvent loop in rebuildRoster() (unregistered/re-registered alongside the existing set) and merged it into HealerMana.lua's existing UNIT_FLAGS/UNIT_HEALTH/UNIT_MAXHEALTH/UNIT_AURA handler branch (same HM.updateUnit(unit) + refreshDisplay() call, no new logic needed since updateUnit already re-reads both UnitIsConnected and UnitIsDeadOrGhost every time).
  - Notes: Not yet verified in-game — but this was found by comparing to Blizzard's own authoritative default-UI implementation of the exact same "show Dead status" feature, not guessed. The auto-detector from PROMPT 63 is still in place and will confirm automatically if this was the actual fix (silent from here means fixed; another AUTO-DEBUG print means there's still a gap). Ran luacheck — 0 new warnings.
- PROMPT 63
  - Intent: Per user request, replace the need to manually run /hm regendebug mid-fight with an automatic background check that catches the dead-status bug (PROMPT 62) on its own
  - Files changed: Core.lua, HealerMana.lua, docs/TODO.md
  - Result: Added HM.autoDetectDeadMismatch() (Core.lua) — compares UnitIsDeadOrGhost(unit) (live) against healerData[unit].dead (stored, what the renderer actually reads) for every tracked healer. Wired to a new 1-second C_Timer.NewTicker in HealerMana.lua's ADDON_LOADED (alongside the existing 0.5s refreshDisplay ticker), only running when healerData is non-empty. Deliberately NOT a print-every-tick design (would spam chat for as long as a mismatch persists) — a reportedDeadMismatch table tracks which units have already been reported, so each mismatch onset prints exactly once and resets automatically once live/stored agree again, ready to report a fresh mismatch later. The printed message points back to /hm regendebug for the fuller per-unit diagnostic (aura checks, regenState, etc.) if more detail is needed once a mismatch is caught.
  - Notes: This is a TEMP diagnostic addition (added to TODO.md for removal once the underlying dead-status bug is actually fixed and confirmed) — its entire purpose is to surface the exact moment/unit of the next occurrence automatically, without the user needing to catch it manually mid-fight. Ran luacheck — 0 new warnings.
- PROMPT 62
  - Intent: User reports a healer dying no longer shows as "Dead" on the tracker (bar stays visible showing a mana %, not pruned/vanished). Did a full fresh review of the entire dead-detection pipeline (Core.lua event registration, HM.updateUnit, HealerMana.lua's dispatcher, both UI.lua render branches) — everything reads structurally correct and unchanged in a way that should cause this, so rather than guess at a fix for code that looks right, extended the existing live-vs-stored debug pattern (which already correctly diagnosed the Innervate data-vs-render gap) to cover dead status too.
  - Files changed: Commands.lua
  - Result: /hm regendebug now also prints liveDead (a fresh UnitIsDeadOrGhost(unit) check) alongside storedDead (healerData[unit].dead, what the renderer actually reads) for every reported unit, using the exact same "is the API detection right vs. is the event-driven update into stored data right" split that worked for Innervate.
  - Notes: Needs the user to run /hm regendebug while a healer is actually dead and report both values. If liveDead=true but storedDead=false, the bug is in event wiring (UNIT_HEALTH/UNIT_FLAGS not firing or not reaching updateUnit) — same class of bug as PROMPT 53's original fix, possibly regressed by something since. If liveDead=false too, UnitIsDeadOrGhost itself isn't detecting it for that unit in this content, which would be a very different (and more surprising) problem. Ran luacheck — 0 new warnings.
- PROMPT 61
  - Intent: User asked to research what current-season crafted mana-restoring items (like "mana buns") players actually use, and whether detection covers them
  - Files changed: Core.lua, docs/TODO.md
  - Result: Research revealed a real gap: current Cooking-profession "Teas" (the modern equivalent of old mana buns) apply a buff named after the tea itself (e.g. "Mana Lily Tea"), in a separate buff slot from food — not "Refreshment"/"Drink" at all, so the existing check was silently missing them. Confirmed 2 of ~5 tea names via research (Mana Lily Tea, Argentleaf Tea); added both to HM.FOOD_DRINK_NAMES (now {"Refreshment", "Drink", "Mana Lily Tea", "Argentleaf Tea"}). Also surfaced a hard limitation with no code fix: crafted mana potions restore mana instantly with no persistent buff at all, so aura-presence detection can never see them — fundamentally different mechanism, not a gap that can be closed. Per user's choice, added the 2 confirmed names now rather than blocking on the full list, since /hm regendebug already provides the exact same verify-live workflow that correctly identified Refreshment and Drink earlier — added a TODO to fill in the remaining tea names as they get tested.
  - Notes: Not yet verified in-game for the 2 new tea names specifically. Ran luacheck — 0 new warnings.
- PROMPT 60
  - Intent: Fix bug reported by user — in a Delve, several non-healers stay visible on the tracker instead of getting pruned out, despite the addon being able to correctly detect actual healer role.
  - Files changed: Core.lua, HealerMana.lua
  - Result: Traced to an interaction between two existing pieces: (1) PROMPT 49's Delve fix tentatively includes any non-player unit whose role is unresolved (since Delves never set roles via LFG), then prunes non-healers once an async spec inspection resolves their real role; (2) HealerMana.lua's GROUP_ROSTER_UPDATE handler had zero debounce — every single firing independently scheduled its own rebuildRoster() call via C_Timer.After(0.5, ...), with no coordination between overlapping timers. Delves are documented (from this addon's own earlier Delve research) to fire GROUP_ROSTER_UPDATE repeatedly — originally attributed to the AI companion's (Brann Bronzebeard) spec/role state syncing as roster events, but the user corrected this: the companion this season is a different character, not Brann. The underlying observed behavior (repeated GROUP_ROSTER_UPDATE firing in delves) and the fix below are unaffected either way, since they don't depend on which specific companion causes it. Since rebuildRoster() wipes healerData and re-evaluates every unit from scratch each time it runs, repeated firings kept resetting the tentative-include-then-prune cycle before inspection could resolve and prune the false positives — so non-healers correctly identified and pruned in one rebuild would just get tentatively re-added in the next one moments later, appearing to "never" get pruned even though the underlying detection was working correctly per unit-check. Fixed with two changes: added a resolvedRoleByName cache (Core.lua, keyed by unit name) inside resolveUnitRole() — once a unit's role is confirmed via a successful spec inspection, every future call for that same name returns the cached role instantly, so isHealer()/pruneNonHealers() never need to tentatively re-include an already-confirmed-non-healer again regardless of how many times rebuildRoster() re-runs; and added a rosterUpdateQueued debounce flag (HealerMana.lua) so overlapping GROUP_ROSTER_UPDATE/ROLE_CHANGED_INFORM events collapse into a single pending rebuild instead of stacking up independent timers, reducing unnecessary rebuild churn generally (not just in Delves).
  - Notes: Not yet verified in-game. The role cache is the primary fix (makes the bug impossible regardless of event frequency); the debounce is a secondary, generally-good improvement. Cache is never invalidated (persists for the WoW session, same pattern as the existing classSpecIcon cache) — a mid-session respec by another player would show stale role info until relog/reload, an accepted edge-case tradeoff matching the addon's existing simplicity-first style. Ran luacheck — 0 new warnings.
- PROMPT 59
  - Intent: User clarified "Food & Drink" (from PROMPT 55's original research) was never a confirmed real buff name — they'd used the phrase as a general/colloquial term, not a WoW spell name. Removed rather than kept "just in case."
  - Files changed: Core.lua
  - Result: HM.FOOD_DRINK_NAMES trimmed from {"Refreshment", "Drink", "Food & Drink"} to {"Refreshment", "Drink"} — the two names actually verified (one user-tested live, one confirmed via Wowhead/Warcraft Wiki research). Commands.lua's /hm regendebug needed no changes since it already iterates HM.FOOD_DRINK_NAMES directly rather than hardcoding the list.
  - Notes: Not yet verified in-game for the "Drink" case specifically. Ran luacheck — 0 new warnings.
- PROMPT 58
  - Intent: User asked whether the eating/drinking detection would pick up ANY mana-restoring consumable, or only the specific "mana bun" item already tested. Researched rather than assumed, since this affects real feature coverage.
  - Files changed: Core.lua
  - Result: Confirmed via Wowhead/Warcraft Wiki that WoW does NOT use one universal eat/drink buff — "Refreshment" is specifically the combined health+mana buff for items designed to restore both at once (what the tested mana bun triggers); plain mana-only drinking (water, mana-only vendor drinks) instead applies a separate "Drink" aura, which the addon wasn't checking for at all. Added "Drink" to HM.FOOD_DRINK_NAMES (now {"Refreshment", "Drink", "Food & Drink"}), checked in that order. Deliberately did NOT add "Food" (health-only regen, confirmed separate from Drink/Refreshment) since it's irrelevant to a mana tracker. Separately, user's own live /hm regendebug test (while actually drinking) showed storedRegenState=drinking matching liveRegenState=drinking — first direct confirmation that the event-driven update into the tracker's real data (not just an on-demand check) is actually working, which is a meaningfully different/better signal than what PROMPT 57 had for the Innervate case.
  - Notes: Not yet verified in-game for the "Drink"-only case specifically (only "Refreshment" via mana bun has been live-tested so far). Since storedRegenState is now confirmed correct for at least one case, the remaining open question for Innervate specifically is narrower than before — could be a genuine rendering-only bug, or could just be that the user checked the icon after Innervate's short 8-second window had already ended. Ran luacheck — 0 new warnings.
- PROMPT 57
  - Intent: User tested /hm regendebug live — Innervate (29166, user-confirmed correct ID) detection worked (regenState=innervate printed correctly) but the icon never visually swapped in the tracker; separately, user reported the eating/drinking buff is actually named "Refreshment" in the current client, not "Food & Drink" as PROMPT 55's research assumed, with no known ID.
  - Files changed: Core.lua, Commands.lua
  - Result: Replaced the guessed FOOD_DRINK_SPELL_ID (192002, now confirmed likely wrong/stale) with name-based lookup via C_UnitAuras.GetAuraDataBySpellName(unit, name, "HELPFUL") — equally valid/unrestricted under 12.1.0 per the same API-changes documentation that covers spell-ID lookups, so no loss of reliability. Checks HM.FOOD_DRINK_NAMES = {"Refreshment", "Food & Drink"} in order (user-confirmed name first, classic name kept as a fallback in case it's still a distinct buff in some other context). HM.foodDrinkIcon is no longer a guessed static ID lookup — it's now resolved dynamically from the actual matched aura's own `.icon` field the first time either name is found, then cached, removing the dependency on knowing a correct spell ID at all for the drinking case. For the still-unexplained Innervate icon-swap miss (detection confirmed working via debug output, icon still didn't show): upgraded /hm regendebug to report both a fresh on-demand check (liveRegenState, exactly what the old version showed) and the value actually stored in healerData/read by the renderer (storedRegenState) side by side, plus per-name ok/type results for the drink check — this distinguishes "detection works but the event-driven update into healerData never fires" from "it's stored correctly but doesn't render," which the prior debug output couldn't tell apart.
  - Notes: Not yet verified in-game — need the user to re-run /hm regendebug while Innervate is actively on them and report both the live and stored values this time; if stored stays nil while live shows innervate, the bug is in the UNIT_AURA event-driven refresh path (Core.lua rebuildRoster/updateUnit), not detection or rendering. Ran luacheck — 0 new warnings.
- PROMPT 56
  - Intent: Per user request, add temporary slash commands to test the new Innervate/Food & Drink detection in real play, easy to remove once confirmed working
  - Files changed: Core.lua, Commands.lua, docs/TODO.md
  - Result: Exposed the previously-local INNERVATE_SPELL_ID/FOOD_DRINK_SPELL_ID constants as HM.INNERVATE_SPELL_ID/HM.FOOD_DRINK_SPELL_ID so Commands.lua doesn't duplicate the magic numbers. Added two commands, both explicitly labeled "TEMP" in their own output and in printHelp(): `/hm regendebug` prints, for every tracked healer (or every party/raid unit if none tracked yet), the raw pcall success + returned type from both spell-ID aura queries plus the final HM.getRegenState() result — this is the direct real-data test of whether 12.1.0 actually returns usable (non-nil, non-error) data for another unit's Innervate/Food&Drink, separate from whether the UI renders it correctly. `/hm raidregen` reuses the existing test-mode roster generator (3 fake healers) and force-sets regenState directly on two of them (test1=innervate, test2=drinking) so the icon-swap rendering itself can be visually confirmed instantly without needing a real druid or someone actually eating — restores via the existing `/hm raid off`, no new restore logic needed. Added a TODO.md entry so these don't get forgotten and left in permanently.
  - Notes: Not yet verified in-game. Ran luacheck — 0 new warnings.
- PROMPT 55
  - Intent: Add Innervate and eating/drinking detection, per user request, after research (earlier this session) found the addon's own prior removal of this exact feature (old PROMPT 6, "no addon workaround possible") was likely outdated — Patch 12.1.0 (shipped Aug 11, 2026) reopened spell-ID-based single-aura queries even on other units during M+/raid. User chose to build now and verify through real play rather than a separate pre-check, given the detection only needs presence (not any of the historically-secret sub-fields like caster/duration).
  - Files changed: Core.lua, HealerMana.lua, UI.lua, Config.lua
  - Result: Core.lua: added INNERVATE_SPELL_ID (29166) and FOOD_DRINK_SPELL_ID (192002) constants, HM.innervateIcon/HM.foodDrinkIcon (resolved once at file-load via C_Spell.GetSpellTexture, a static icon lookup with no secrecy concerns), and HM.getRegenState(unit) — checks C_UnitAuras.GetUnitAuraBySpellID(unit, spellID) for each spell via pcall, treating any successfully-returned table as "aura present" without reading any individual field (duration/caster/etc. are what the old secret-value restrictions targeted; presence alone sidesteps that entirely, so no issecretvalue() checking was needed here). Wired into snapshotUnit (initial roster build) and HM.updateUnit (event-driven refresh) as a new d.regenState field ("innervate"/"drinking"/nil). Registered UNIT_AURA per-unit in rebuildRoster's existing unregister/register loop; merged it into HealerMana.lua's existing UNIT_FLAGS/UNIT_HEALTH/UNIT_MAXHEALTH handler branch (same HM.updateUnit(unit) + refreshDisplay() call) rather than a new branch, since UNIT_AURA's event payload is itself secret in 12.1.0 and is only useful here as a "something changed, re-poll directly" trigger anyway. UI.lua: icon-style renderBar now overrides the healer's icon texture with HM.innervateIcon/HM.foodDrinkIcon when regenState is set (falls back to the normal spec icon if the texture lookup ever came back nil), per user's explicit choice of "replace the icon" over a badge overlay or border/text treatment. Scoped to Icon display style only, since Bar style has no icon element to replace — not yet extended to bars. Added a short Help-tab entry documenting the behavior.
  - Notes: Not yet verified in-game — this is the first real test of whether 12.1.0 actually reopened the API the way research indicated; if the icon never swaps in a real M+/raid despite a druid innervating or someone visibly eating, that's the signal the restriction is narrower/different than researched, not that the code is broken. Ran luacheck — 0 new warnings.
- PROMPT 54
  - Intent: User asked for a smoke test of every option, checking for errors. No in-game client access available this session, so did the most rigorous static equivalent instead: full luacheck, .toc validation, a cfg-key/DEFAULTS cross-check, and a manual trace of every control's callback path across all 7 config tabs and both display styles.
  - Files changed: UI.lua, docs/TESTING.md
  - Result: Found and fixed one real (if harmless) bug: icon-style renderBar's `getFillColor(data, pct)` call still passed a 2nd argument left over from the Mana Threshold removal (PROMPT 45), even though getFillColor(data) is single-arg now — Lua silently drops extra call args so this never errored, but it was dead/misleading code implying pct still influenced fill color. Fixed to getFillColor(data), matching the bar-style call site that was already correct. Everything else checked out: every cfg.X used anywhere in the addon has a matching DEFAULTS entry (grep diff, zero mismatches), all conditionally-created widgets (barTextureDropdown, fontDropdown) are nil-guarded everywhere they're referenced, switchTab's paneRefresh[key]() covers all 7 tab keys with no gaps, stretch/hide logic in layoutRows is consistent across every pane, and no duplicate frame-name or double-construction risks.
  - Notes: Explicitly told the user this is not equivalent to an in-game pass — no way to click through the real UI, trigger real events (INSPECT_READY, UNIT_HEALTH, etc.), or catch visual/layout issues this way. Logged the pass in docs/TESTING.md per the project's testing workflow. Ran luacheck — 0 new warnings (same 2 pre-existing unrelated items as always).
- PROMPT 53
  - Intent: Fix bug reported by user — a healer stuck showing "Dead" after being resurrected. This exact bug was reported and worked on once before (an earlier prompt moved UNIT_FLAGS to per-unit RegisterUnitEvent), but that fix was never confirmed working in-game and the module split/Delve rework since then hadn't touched this code, so it needed fresh investigation rather than assuming the old fix was still correct.
  - Files changed: Core.lua, HealerMana.lua
  - Result: Confirmed the per-unit UNIT_FLAGS registration from the earlier fix was still intact (not a regression) — the real problem is UNIT_FLAGS itself, which isn't actually a death/resurrection signal. Verified via web research against real, actively-maintained addon source (oUF, VuhDo, Grid2 on GitHub) rather than guessing: none of them use UNIT_FLAGS for dead-state tracking; the standard is UNIT_HEALTH (+UNIT_MAXHEALTH), re-checking UnitIsDeadOrGhost() on each fire — UNIT_FLAGS is used elsewhere (bar recolor in oUF) but never as the primary death trigger, and there's no dedicated "resurrection" event since PLAYER_ALIVE/PLAYER_UNGHOST are player-only and INCOMING_RESURRECT_CHANGED only tracks a pending offer, not the actual life-state flip. Added UNIT_HEALTH and UNIT_MAXHEALTH to Core.lua's per-unit RegisterUnitEvent loop in rebuildRoster() (alongside the existing UNIT_POWER_FREQUENT/UNIT_IN_RANGE_UPDATE/UNIT_FLAGS, unregistered and re-registered together as before) and merged them into HealerMana.lua's existing UNIT_FLAGS handler branch (same HM.updateUnit(unit) + HM.refreshDisplay() call — updateUnit already re-reads UnitIsDeadOrGhost every time, so no new logic was needed, just the missing trigger). Kept UNIT_FLAGS registered too rather than replacing it, since it still legitimately covers connection-state changes (updateUnit also re-reads UnitIsConnected) and costs nothing to keep as a secondary trigger.
  - Notes: Not yet verified in-game — this one specifically needs a real death+resurrection test to confirm, not just a lint pass. Ran luacheck — 0 new warnings.
- PROMPT 52
  - Intent: Fix bug reported right after PROMPT 51 shipped — Help tab sentences were getting cut off on the right instead of wrapping; also two small title-bar tweaks (deepen the new "Mana" blue, chosen initially as a light CSS-lightblue ADD8E6)
  - Files changed: Config.lua
  - Result: Root cause: FontStrings anchored via two opposite points (TOPLEFT+TOPRIGHT) don't reliably derive a wrap width from the anchors the way Frames do — SetWordWrap(true) alone wasn't enough, and the ScrollFrame silently clipped the overflowing unwrapped text at its right edge, reading as "cut off." Fixed by giving each Help tab FontString an explicit pixel width (helpPane:GetWidth(), with a 260px fallback for the theoretical case it's queried before ever being sized) via SetWidth() instead of a TOPRIGHT anchor, right before measuring GetStringHeight() each refresh — the standard, reliable pattern for wrapped FontString text, chosen over widening the whole panel (offered as the other option) since it doesn't disturb every other tab's layout. Title bar: "Mana" recolored from ADD8E6 (CSS lightblue) to 3399FF, a deeper/more saturated blue, incidentally close to WoW's own classic mana-orb blue.
  - Notes: Not yet verified in-game. Ran luacheck — 0 new warnings.
- PROMPT 51
  - Intent: Two user requests together — (1) add a 7th config tab with detailed instructions on using the addon and what each existing tab does, and (2) recolor the title bar text so "Healer" keeps its current color and "Mana" becomes light blue
  - Files changed: Config.lua, docs/ADDON_CONTEXT.md
  - Result: (1) Added a "Help" tab (static reference content, no interactive controls) built from a HELP_SECTIONS data table (title/header/body kind per entry) rendered as stacked FontStrings — title uses GameFontNormalLarge/PRIMARY, headers use GameFontHighlightSmall/ACCENT (matching the old makeGroup header style before it was removed), body text uses GameFontHighlightSmall/PRIMARY for readability as the tab's actual content rather than secondary label text. paneRefresh.help measures each FontString's wrapped height via GetStringHeight() (queried right after anchoring, which fixes its width) to stack them and compute the pane's total height, reusing the existing SECTION_GAP constant before each header for visual separation — same dynamic-height/scrolling mechanism every other tab already uses, so this tab scrolls gracefully despite being by far the longest. Content covers: what the addon does, minimap icon left/right-click behavior, all 4 slash commands, and a one-paragraph summary of each of the other 6 tabs plus how to move/reset the frame's position. (2) titleLbl's text changed from plain "HealerMana" to "Healer|cffADD8E6Mana|r" — an inline color escape override on just the "Mana" substring (light blue, CSS "lightblue" hex ADD8E6), while the FontString's base SetTextColor (still PRIMARY) continues to color "Healer".
  - Notes: Not yet verified in-game. Ran luacheck — 0 new warnings. Help tab content is static prose, not read from cfg, so it won't drift if defaults change, but could go stale if a tab's controls change materially — worth a periodic read-through when touching other tabs.
- PROMPT 50
  - Intent: User clarified a follow-up to PROMPT 48's style-gated tab content — they meant the "Background & Border" tab's own label should relabel to just "Background" or "Border" depending on display style, not only its contents
  - Files changed: Config.lua
  - Result: Added updateBgBorderTabLabel(), which sets tabButtons.bgBorder.lbl to "Background" when cfg.displayStyle == "bar" or "Border" otherwise; called right after the tab rail is built (so it overwrites the static "Background & Border" placeholder from TAB_DEFS before the panel is ever shown), from displayStyleDropdown's onSelect (alongside the existing paneRefresh calls), and from f:SetScript("OnShow", ...) in case displayStyle changed via slash command between panel opens. Had to forward-declare `local updateBgBorderTabLabel` near panes/paneRefresh/tabButtons at the top of createConfigFrame and assign it without `local` later, since displayStyleDropdown's callback (built early, in the General pane section) references it — a plain local closed over by that closure, not a table field lookup like paneRefresh.xxx(), so it needed the same forward-declare treatment as gapSlider back in the original tabbed-panel redesign.
  - Notes: Not yet verified in-game. Ran luacheck — 0 new warnings.
- PROMPT 49
  - Intent: Fix bug reported by user — HealerMana shows nothing at all in Delves (The War Within's solo/small-group content), even with a real healer in a 2-4 real-player group
  - Files changed: Core.lua, HealerMana.lua
  - Result: Root-caused via web research (Warcraft Wiki, a WoWInterface addon-dev thread on detecting delves, and Cell's production raid-frame source) rather than guessing, per this project's rule against guessing WoW API behavior: Delves never go through LFG's role-select step, so `UnitGroupRolesAssigned(unit)` can return "NONE" for every group member even while someone is actively healing — and HealerMana's roster filter required exactly `UnitGroupRolesAssigned(unit) == "HEALER"` to include anyone, so an all-"NONE" delve group produces an empty roster. Confirmed the diagnosis with the user (2-4 real players, a real healer present, still zero bars) before writing a fix. Replaced the old one-line isHealer() with resolveUnitRole(unit): returns the assigned role directly when it's HEALER/TANK/DAMAGER (today's fast path, unchanged for normal LFG/LFR groups and raids), and when the role is unassigned falls back to specialization — GetSpecializationInfo(GetSpecialization()) for the player (synchronous, always known instantly) or GetInspectSpecialization(unit) + GetSpecializationInfoByID(specID) for other units (async, needs an inspection round-trip; confirmed the 5th return value of both GetSpecializationInfo/GetSpecializationInfoByID is role by cross-checking against this file's own already-working icon lookups, which already prove position 4 is icon). isHealer(unit) now tentatively includes any non-player unit whose role is still unresolved (not yet inspected) rather than excluding it, so a delve group gets everyone added optimistically at rebuildRoster time; the existing per-unit staggered NotifyInspect loop (unchanged) then resolves each one, and a new HM.pruneNonHealers() — called from the INSPECT_READY handler in HealerMana.lua, right before the existing specIcon-refresh loop — removes any tentatively-included unit whose now-resolved role isn't HEALER.
  - Notes: Not yet verified in-game — please confirm bars now appear during a delve run. Known tradeoff: in a delve (or any group where roles aren't explicitly assigned), non-healer party members may flash briefly (up to ~0.5-2s, following the existing staggered inspect delay) before being pruned down to just the real healer(s) — accepted as a minor, self-correcting visual blip in exchange for actually working in delves at all. Normal LFG-queued dungeons/raids are unaffected since their roles are always explicitly assigned, so the fast path (unchanged) still applies with zero behavior change. Ran luacheck — 0 new warnings.
- PROMPT 48
  - Intent: Per user preference, remove the border entirely from Bar display style — keep it only on Icons
  - Files changed: UI.lua, Config.lua
  - Result: UI.lua createBar's bar-mode branch no longer creates bar.border at all (also removes the SetFrameLevel ordering fix from PROMPT 47, since there's nothing left to order against fill). renderBar's bar-mode branch dropped its border-color block entirely; kept `local cc = CLASS_COLOR[data.class]` since it's still read below for name/percent class-color. Icon display style's border is completely untouched. Config.lua's Background & Border tab: "Class color border" toggle and the Border Color swatch now only appear when cfg.displayStyle == "icon" (previously always shown); Background Color swatch keeps its existing bar-only condition. In bar mode the tab now shows only the background swatch; in icon mode it shows the border toggle (+ swatch when class-color is off) — never both border and background controls in the same view, matching what's actually rendered for the current style.
  - Notes: Not yet verified in-game. Ran luacheck — 0 new warnings. If the Background & Border tab title now reads oddly since it's function-split by style, that's a possible follow-up (e.g. renaming per-style), but left as-is since the tab still legitimately covers "background" and "border" concepts, just gated appropriately now.
- PROMPT 47
  - Intent: Fix bug reported by user during test-mode preview — in Bar display style, the mana fill visually covers part of the bar's border instead of sitting under it
  - Files changed: UI.lua
  - Result: Root cause: bar.fill (a StatusBar) and bar.border (a BackdropTemplate Frame) are both plain sibling child frames of `bar`, created back-to-back with no explicit SetFrameLevel — so both default to the same frame level, leaving their relative draw order over each other unspecified rather than guaranteed. (Icon display style doesn't have this problem: there, the icon/tint are just texture regions drawn directly on `bar` itself, not competing sibling frames, so the border-frame's default child-level naturally draws above them.) Fixed by explicitly calling `bar.border:SetFrameLevel(bar.fill:GetFrameLevel() + 1)` right after building the border's backdrop, guaranteeing the border always renders above the fill regardless of creation-order tie-breaking.
  - Notes: Not yet verified in-game — please confirm the border now shows fully around the bar (try a few different Bar Opacity values too, since that's the other bar-style visual change from recent prompts). Ran luacheck — 0 new warnings.
- PROMPT 46
  - Intent: Now that the config panel is fully tabbed and comprehensive, revisit the minimap icon's right-click quick-menu (Style/Arrange/Sort/Lock/Preview/Reset Position/Open Settings) since most of it just duplicated paths already in the config tabs. Discussed options with user; they chose the simplest: right-click directly toggles the 5-healer test preview, no popup menu at all.
  - Files changed: Minimap.lua, Config.lua, docs/ADDON_CONTEXT.md
  - Result: Rewrote Minimap.lua — removed the entire custom popup-menu system (buildRows, ensureMenu, showMenu, toggleMenu, hideMenu, the menu/overlay frames and ROW_H/ROW_W constants), replaced with a single togglePreview() function mirroring Commands.lua's existing `/hm raid <n>`/`/hm raid off` toggle logic (same HM.testModeActive flag, HM.generateTestRoster/HM.rebuildRoster calls, matching chat-print style). OnClick handler: left-click still opens settings, right-click now calls togglePreview() directly instead of showing a menu. Tooltip text updated to describe the new right-click behavior. Style/Arrange/Sort/Lock/Reset Position/Open Settings are no longer reachable from the minimap icon at all — Style is in General, Arrange (Layout Mode) is in Layout, Sort is in Sort & Visibility, Lock is in General, Reset Position is the config panel's own footer button, and Open Settings is just left-click itself; nothing was actually lost, just de-duplicated to one path each. Since Minimap.lua no longer touches Config.lua's flatBtn/addBorder/uiColors, removed those three now-fully-unused cross-module exports from Config.lua (kept the internal locals Config.lua itself still uses) and the now-orphaned GROUP color constant that only existed to feed the removed uiColors export.
  - Notes: Not yet verified in-game. Ran luacheck — 0 new warnings.
- PROMPT 45
  - Intent: Two user requests in one pass — (1) remove the Mana Threshold fill-color mode ("not working and i dont see the need as of yet"), and (2) the config panel felt cramped; group same-type controls together (checkboxes with checkboxes, dropdowns with dropdowns, swatches with swatches) with real spacing between each sub-section, across all 6 tabs
  - Files changed: HealerMana.lua, UI.lua, Config.lua
  - Result: (1) Removed manaColorLow/Mid/High DEFAULTS keys, UI.lua's getThresholdColor() function, and getFillColor's threshold branch (now just two cases: custom or class-with-custom-fallback; also dropped the now-unused `pct` parameter from getFillColor's signature — both call sites updated). Config.lua: removed "Mana Threshold" from the Fill Color Mode dropdown and deleted the Low/Mid/High swatch widgets and their OnShow refresh calls entirely, not just hidden — matches "for now" framing (temporary removal of a broken feature, not a permanent architecture decision, so no migration path was built for old saved barColorMode="threshold" profiles; they'll silently render as class color via the same fallback path real class-less data already uses). (2) Added a `group` field to every row descriptor (e.g. "dropdown"/"toggle"/"slider"/"swatch") and a SECTION_GAP (14px) in the shared layoutRows() helper: when consecutive rows' group differs, an extra gap is inserted before continuing — the row-list construction per tab, not layoutRows itself, is what guarantees same-type rows stay contiguous (rebuilt all 6 paneRefresh functions to emit rows pre-clustered in a consistent Dropdowns → Toggles → Sliders → Swatches order). Also widened all four base row-height constants for general breathing room (toggle 22→26, dropdown/slider 44→48, swatch 24→28) and pane top/bottom padding (6/12→10/20). Sort & Visibility tab reuses the same `group` mechanism semantically (not by widget type, since it's all toggles) to put a gap between its 2 sort-order toggles and 7 visibility toggles. Bumped the config frame from 480x500 to 480x560 since the added spacing pushed the worst-case tab (Text, with every conditional row showing) close to the old available height.
  - Notes: Not yet verified in-game. Ran luacheck after both changes — 0 new warnings.
- PROMPT 44
  - Intent: Player noticed the "Bar Color" tab still showed while Icons display style was selected, and that background/color changes don't read the same on icons as on bars. Root-caused with the user: fill/tint color already applies to both styles (icon tint is just a subtler 22%-alpha overlay vs. a full bar fill), but Background Color has ZERO visual effect in icon mode since bar.bg sits fully behind the opaque, same-sized icon texture. User asked to (1) hide the background swatch in icon mode, (2) keep the tint subtle as-is but add opacity sliders for it, and (3) add an opacity slider for the existing out-of-range dim effect too.
  - Files changed: HealerMana.lua, UI.lua, Config.lua
  - Result: Added DEFAULTS keys barFillOpacity (1.00), iconTintOpacity (0.22 — matches the prior hardcoded constant, zero default visual change), outOfRangeOpacity (0.35 — matches the prior hardcoded constant). UI.lua: bar-style renderBar's three SetStatusBarColor calls (dead/DC/live) now pass cfg.barFillOpacity as the 4th alpha argument (StatusBar:SetStatusBarColor(r,g,b,a) confirmed as real, current API via Warcraft Wiki before using it, per project rule against guessing WoW APIs); icon-style's bar.tint:SetAlpha (creation + all 3 render branches) now reads cfg.iconTintOpacity instead of the hardcoded 0.22; the dimOutOfRange block's two hardcoded 0.35 alpha values (secret-safe SetAlphaFromBoolean branch and the plain SetAlpha branch) now read cfg.outOfRangeOpacity. Config.lua: renamed the "Bar Color" tab to "Fill Color" (and its "Bar Color Mode" dropdown to "Fill Color Mode") since it always governed both styles, just wasn't labeled that way; added a "Bar Opacity" slider (bar mode) / "Icon Tint Opacity" slider (icon mode) to that tab, shown one-or-the-other based on cfg.displayStyle; Background & Border tab's background swatch now only shows when cfg.displayStyle == "bar"; General tab's "Out of Range Opacity" slider only shows when "Dim when out of range" is on. displayStyleDropdown's onSelect now also calls paneRefresh.barColor() and paneRefresh.bgBorder() (previously only paneRefresh.layout()) so both newly-conditional panes react immediately to a style switch, even while viewing a different tab — safe since layoutRows() only pushes to the visible scroll content when its pane is the active one.
  - Notes: Not yet verified in-game. Ran luacheck — 0 new warnings. Opacity sliders use a 0-100 integer scale (stored /100 as 0.0-1.0) rather than raw 0-1, matching how "percent"-style controls read more naturally to players than a 2-decimal float would.
- PROMPT 43
  - Intent: Per user request, add a class-color option for the percent text, matching the existing "Class color name" option for name text
  - Files changed: HealerMana.lua, UI.lua, Config.lua
  - Result: Added pctClassColor DEFAULTS key (false, preserving today's always-custom-color behavior). UI.lua's two percent-text-color sites (bar-mode and icon-mode renderBar, both already have `cc = CLASS_COLOR[data.class]` in scope) now check `cc and cfg.pctClassColor` first, falling back to cfg.pctCustomColor exactly like name text does; Dead/DC status text colors are untouched (fixed indicator colors, not themeable, same as name text). Config.lua Text tab: added a "Class color percent" toggle right after "Show % symbol", and the Percent Text Color swatch now only shows when the toggle is off (mirrors the Name Text Color swatch's relationship to "Class color name").
  - Notes: Not yet verified in-game. Ran luacheck — 0 new warnings.
- PROMPT 42
  - Intent: Redesign the settings panel per user request — left-side vertical tabs instead of the two-column scrolling page, with each tab showing only controls relevant to current settings (e.g. no bar-specific Layout controls while Icons display style is selected)
  - Files changed: Config.lua, docs/ADDON_CONTEXT.md
  - Result: Confirmed with user beforehand: 6 tabs (General, Layout, Bar Color, Background & Border, Text, Sort & Visibility) and a narrower ~480px frame (was 700x560, now 480x500). Fully rewrote createConfigFrame: removed the two-column placeGroup/makeGroup/leftColumn/rightColumn machinery and the recomputeContentHeight cross-column juggling from PROMPT 39-40 entirely, replaced with a left tabRail (new makeTabButton widget — accent-bar + highlight active state) and a single right-hand scroll area holding 6 pane frames (one per tab, all children of the same scroll content, only the active one shown/hidden via switchTab()). Each pane has its own paneRefresh[key]() function that builds a `rows` array (widget + row height + optional stretch=false for toggles, which self-size instead of stretching full-width like dropdowns/sliders/swatches do) and calls a shared layoutRows() helper to position/show/hide rows and resize the pane; switchTab() calls the target pane's refresh and resizes the scroll content to just that pane's height — no more coordinating two columns' heights against each other. Reassigned two settings during the split, since they're now naturally co-located with what they gate: "Class color border" moved from the old Display group into the Background & Border tab (right next to the border swatch it enables/disables) and "Class color name" moved into the Text tab (right next to the name-color swatch) — this also let the old cross-tab forward-declared refreshBgBorderPanel/refreshTextPanel upvalue wiring from PROMPT 39 be deleted; toggling either now just calls paneRefresh.bgBorder()/paneRefresh.text() directly since both live in the same pane. Layout Mode dropdown moved from General into the Layout tab (previously in General only because General was built before Layout existed as a group). Added a new reactive relevance rule per user's explicit ask: the Text tab now hides "Long Name Handling", "Class color name", and its name-color swatch entirely when "Show player name" is off (previously those stayed visible but inert). Removed makeGroup (now fully unused — panes have no bordered box/header, the tab rail label already conveys category) and updated docs/ADDON_CONTEXT.md's Config.lua description accordingly.
  - Notes: Not yet verified in-game. Ran luacheck after the rewrite — 0 new warnings, same 2 pre-existing unrelated items as before. Checked worst-case pane height (Text tab with every conditional row showing) fits the new panel without needing to scroll (~324px content vs. ~400px available), so scrolling is a rare-case fallback, not the norm. Tab selection is not persisted across closes (always reopens on General) — not requested, easy follow-up if wanted.
- PROMPT 41
  - Intent: Per user request, stop /hm raid test mode from flagging its last fake entry as dead — not useful for previewing bar-color/text styling since a dead bar skips the fill-color/pct-text render path entirely
  - Files changed: Core.lua
  - Result: generateTestRoster's dead field changed from `(i == n)` to always `false`. testPct spread (added PROMPT 40) and dead-status testing are otherwise independent features; this only removes the always-on last-entry-is-dead behavior from PROMPT 9.
  - Notes: Not yet verified in-game. Ran luacheck — 0 new warnings. If dead-state visuals ever need testing again, that's now unreachable from /hm raid and would need a separate opt-in.
- PROMPT 40
  - Intent: Fix bug reported by user right after PROMPT 39 shipped — `/hm raid <n>` test bars all showed 0% mana, making the new bar-color threshold modes impossible to preview
  - Files changed: Core.lua, UI.lua
  - Result: Root cause: generateTestRoster's fake entries reuse unit="player" (by design, per PROMPT 9, so the live isUnitInRange/readUnitPctRaw calls always get a real valid unit token) — so every fake bar showed the user's own actual mana %, which reads 0 on any non-mana-pool class. Added a testPct field to each fake healer (spread evenly 5-95 across the n entries, so a spread of test bars always spans all three Low/Mid/High threshold bands regardless of n), and changed both renderBar pct reads (`local pct = HM.readUnitPctRaw(data.unit)`, bar-mode and icon-mode) to `data.testPct or HM.readUnitPctRaw(data.unit)` — real roster entries have no testPct field so they're completely unaffected.
  - Notes: Not yet verified in-game. Ran luacheck after the fix — 0 new warnings.
- PROMPT 39
  - Intent: Player-requested feature — make bar color and text properties editable, instead of the fixed class-color/mana-threshold/white scheme
  - Files changed: HealerMana.lua, Core.lua, UI.lua, Config.lua
  - Result: Added DEFAULTS keys barColorMode ("class"/"custom"/"threshold"), barColorCustom, manaColorLow/Mid/High (the icon-tint mana-threshold colors, previously hardcoded module constants in UI.lua, now cfg-driven and shared with bar mode), bgColor (with alpha), borderCustomColor, nameCustomColor, pctCustomColor, fontFace (LSM font key or false for default), fontSizeOverride, nameFontSizeManual, pctFontSizeManual. UI.lua: replaced the old getManaColor (icon-tint only) with getFillColor(data, pct), a single function used by BOTH display styles that switches on cfg.barColorMode — "class" uses CLASS_COLOR with a custom-color fallback for non-class data, "custom" always uses barColorCustom, "threshold" evaluates the low/mid/high cfg colors per-bar from that bar's own actual mana %, with the existing secret-value/unknown fallback tones preserved. This is a behavior change for icon mode specifically: previously icon tint was unconditionally mana-threshold-colored; it now defaults to "class" (matching bar mode's prior default and the border/name class-color toggles) with threshold available as an explicit opt-in. Border fallback color (when "Class color border" is off) and name/percent text colors (when not class-colored) now read from the new custom-color cfg fields instead of hardcoded 0.85/1,1,1 literals; dead/DC status colors are untouched (kept as fixed indicator colors, not themeable). Added HM.getFontPath() (Core.lua) resolving cfg.fontFace through LibSharedMedia's "font" media type with a STANDARD_TEXT_FONT fallback, and effectiveNameFontSize/effectivePctFontSize (UI.lua) so cfg.fontSizeOverride's manual sliders take over from the existing auto-scaling (autoFontSizes/autoBarFontSize) at every SetFont call site in both display styles, including the shrink/truncate name-fit helpers. Config.lua: added makeColorSwatch (a themed swatch button opening Blizzard's stock ColorPickerFrame via SetupColorPickerAndShow — confirmed against Blizzard's own live UI source rather than guessed, since the color-field API changed in 10.2.5) and three new reactive groups — "Bar Color" (mode dropdown + conditionally-shown custom/threshold swatches, left column), "Background & Border" (bg swatch always, border swatch only when class-color border is off, left column), and "Text" (LSM font dropdown when available, font-size-override toggle + conditionally-shown manual size sliders, name-color swatch only when class-color name is off, percent-color swatch always, right column). Replaced the old static leftFinalH/content-height formula with a shared recomputeContentHeight() closure so the scroll area always fits whichever combination of dynamically-sized groups (Bar Color, Layout, Text all resize based on mode/toggles) is currently tallest per column.
  - Notes: Not yet verified in-game — pending a smoke test of every new mode/toggle combination (all 3 bar color modes on both display styles, font override on/off, LSM present/absent). Verified via web research (not guessed) that ColorPickerFrame:SetupColorPickerAndShow(info) with swatchFunc/opacityFunc/cancelFunc (reading color back via ColorPickerFrame:GetColorRGB()/GetColorAlpha()) is the current, correct API, and that LibSharedMedia-3.0 supports a "font" media type via LSM:List/Fetch identically to the already-used "statusbar" type. Ran luacheck (WoW 5.1 config) after all edits — 0 new warnings, only the two pre-existing unrelated items (ADDON_NAME unused-var pattern mismatch, GetCursorPosition missing from the curated globals list). Used Blizzard's built-in ColorPickerFrame with explicit user sign-off, since CLAUDE.md otherwise restricts Blizzard UI templates/assets.
- PROMPT 38
  - Intent: Investigate report that only the bottom-right corner of the minimap icon was clickable
  - Files changed: Minimap.lua
  - Result: Verified the icon art (HealerMana_32x32.tga) fills its full canvas — no transparent padding pushing the art into a corner — and confirmed LibDBIcon-1.0 sets no hit-rect insets, so its button frame is already fully clickable by the library's own code. Root cause is almost certainly another installed addon's minimap icon (several others on this system also use LibDBIcon) overlapping ours at the same default angle, leaving only the uncovered sliver responsive to clicks. Hardened HealerMana's button with explicit SetHitRectInsets(0,0,0,0) (defensive, guarantees full-frame hit test) and bumped its frame level by 1 above the library default so it wins click priority over same-library icons it overlaps with.
  - Notes: Practical fix on the player's end: drag the icon (unlocked by default) to an open spot on the minimap ring. Not fully verified in-game — pending confirmation.
- PROMPT 37
  - Intent: Add a minimap icon (using the user-provided HealerMana_32x32.tga) built on a shared LDB library so other addons can interoperate with it; left-click opens settings, right-click opens a quick layout menu; tooltip explains the addon
  - Files changed: Minimap.lua (new), Libs/LibStub, Libs/LibDataBroker-1.1, Libs/LibDBIcon-1.0 (new, embedded from RaiderIO's copies — newest versions found locally, includes AddOn Compartment support), HealerMana.toc, HealerMana.lua, Config.lua, docs/ADDON_CONTEXT.md
  - Result: Minimap.lua registers an LDB launcher data object (so other broker displays like ChocolateBar can pick it up too) and, if LibDBIcon-1.0 is present, a draggable minimap button. Left-click opens the settings panel; right-click opens a custom context menu (built with Config.lua's existing flatBtn/addBorder/uiColors, now exposed via HM) with: display style toggle, arrange (vertical/horizontal/grid) cycle, sort toggle, lock toggle, a 5-healer raid preview toggle (mirrors /hm raid), reset position, and open settings. Tooltip shows the addon name, a one-line description, and left/right-click hints. Position/hide state persists in HealerManaDB.minimap (a dedicated sub-table LibDBIcon owns directly, separate from cfg/saveKey). Added a "Show minimap icon" toggle to the settings panel's General group (9th toggle, was 8) so the icon can be hidden without touching SavedVariables by hand.
  - Notes: Verified library files parse under reference Lua 5.1 (loadfile), and luacheck on HealerMana's own 6 files shows no new warning categories beyond the pre-existing WoW-API/HealerManaDB globals pattern. Libs/ is third-party code — not linted or hand-edited. Icon path is Interface\AddOns\HealerMana\HealerMana_32x32.tga (root, not moved).
- PROMPT 36
  - Intent: Trim CURSEFORGE.md commands table down to just /hm and /hm raid <number>
  - Files changed: CURSEFORGE.md
  - Result: Dropped /hm lock, /hm alpha, /hm class, /hm layout, /hm reset from the player-facing commands table — those are all reachable from the settings window anyway. Updated the "Getting Set Up" step 5 to say "lock from the settings window" instead of the /hm lock command.
  - Notes: This only trims the doc's presented commands; all slash commands still exist and work in Commands.lua, nothing removed from the addon itself.
- PROMPT 35
  - Intent: Rewrite CURSEFORGE.md in plain player-facing language — previous draft was too technical (secret-value/API jargon)
  - Files changed: CURSEFORGE.md
  - Result: Rewrote feature list and how-to-use section to describe what players see/experience rather than implementation details. Dropped /hm debug and /hm range from the commands table (developer-only tools, not relevant to players).
  - Notes: Slash command table kept — that's genuinely useful reference material for players, not "technical."
- PROMPT 34
  - Intent: Remove WeakAuras references from CURSEFORGE.md per user request
  - Files changed: CURSEFORGE.md
  - Result: Dropped the "drop-in replacement for the Raid Mana Healer Castbar WeakAura" line and the "no import strings / broken export" phrasing from the Why section. Reasoning (works reliably under secret-value lockdown) kept, just without naming WeakAuras.
  - Notes: User's framing — WeakAuras itself is broken by Blizzard's secret-value changes, this addon exists as a real alternative, not a WeakAura companion/replacement worth naming.
- PROMPT 33
  - Intent: Write a copy-pasteable CurseForge project-page description
  - Files changed: CURSEFORGE.md (new)
  - Result: Root CURSEFORGE.md added — catchy pitch, feature list, slash command table, quick-start steps. Feature list cross-checked against actual code (not ADDON_CONTEXT.md's stale "eating/drinking/Innervate" entry, which was removed in PROMPT 6 and no longer exists in any .lua file).
  - Notes: Not published anywhere — user pastes this into the CurseForge page manually. Per house rule, publishing itself is never done without an explicit ask.
- PROMPT 32
  - Intent: Add a user-facing CHANGELOG.md at root, separate from this dev-facing log
  - Files changed: CHANGELOG.md (new)
  - Result: Root CHANGELOG.md created with a minimal "1.0.0 - Release" entry, matching the format CurseForge/Wago listings expect (short, player-facing) rather than this file's per-prompt dev detail.
  - Notes: CLAUDE.md stays at root (not moved to docs/) per user's explicit choice, so it keeps auto-loading for future Claude Code sessions.
- PROMPT 31
  - Intent: Bump Interface version to match user's actual client (12.1.0)
  - Files changed: HealerMana.toc, CLAUDE.md, docs/DEV_RULES.md, docs/ADDON_CONTEXT.md
  - Result: Interface version updated 120001 → 120100 (12.0.1 → 12.1.0) across the .toc and all doc references. Explicitly requested by user, per CLAUDE.md's "do not change TOC/interface version unless explicitly asked" rule.
  - Notes: Historical CHANGELOG_DEV.md entries referencing 120001 left as-is (log of past state, not current state).
- PROMPT 30
  - Intent: Split HealerMana.lua (1733 lines, single file) into responsibility-focused modules per DEV_RULES ("split growing files before they become unmanageable")
  - Files changed: HealerMana.lua, Core.lua (new), UI.lua (new), Config.lua (new), Commands.lua (new), HealerMana.toc, docs/ADDON_CONTEXT.md
  - Result: Pure mechanical split, no behavior/feature changes. HealerMana.lua is now the bootstrap only (defaults, saved-var load, event frame + dispatch). Core.lua holds roster/mana/range/spec logic. UI.lua holds the bar pool and display refresh. Config.lua holds the widget helpers and settings panel. Commands.lua holds slash commands and debug dumps. Cross-file state/functions are shared via the standard `local ADDON_NAME, HM = ...` addon table (each file loads with the same shared HM table); cfg/healerData/barPool/eventFrame are aliased once per file since they're mutated in place, not reassigned. Verified via luacheck (0 syntax errors; remaining warnings are pre-existing WoW-API globals missing from the minimal .luacheckrc, not new issues) and a .toc-vs-disk file check.
  - Notes: No Lua 5.1 gotchas introduced (grepped for table.unpack/goto/bitops/os.*/io.*). Load order in .toc: HealerMana.lua, Core.lua, UI.lua, Config.lua, Commands.lua — order isn't load-bearing since inter-module calls only happen inside functions invoked after ADDON_LOADED, once every file has loaded.
- PROMPT 1
  - Intent: Session initialization — read all context docs, populate ADDON_CONTEXT.md with real addon details
  - Files changed: docs/ADDON_CONTEXT.md
  - Result: ADDON_CONTEXT.md updated from placeholder template to reflect actual HealerMana addon (identity, features, architecture, slash commands, SavedVariables, constraints)
  - Notes: TOC Interface 120001 vs CLAUDE.md target 120000 — noted, not changed per rules
- PROMPT 3
  - Intent: Fix eating/drinking/Innervate detection for non-player healers
  - Files changed: HealerMana.lua
  - Result: Replaced non-existent C_UnitAuras.GetAuraDataBySpellID with C_UnitAuras.GetUnitAuraBySpellID(unit, spellID) (added 11.2.5). The old call was dead code — the guard condition was always false, so non-player units always fell through to the unsafe GetBuffDataByIndex fallback. Now correctly detects eating/drinking/Innervate on party and raid members.
  - Notes: GetPlayerAuraBySpellID (player path) was already correct. Fallback retained for pre-11.2.5 builds. Midnight drink IDs confirmed correct (1269919 = Drink, 6% mana/sec).
- PROMPT 4
  - Intent: Fix icon size slider — step 1 per tick, range 30–60
  - Files changed: HealerMana.lua
  - Result: Changed makeSlider call from (24, 96, step=4) to (30, 60, step=1). Default cellSize (52) is within new range.
  - Notes: Saved values outside 30–60 are clamped by existing slider snap logic.
- PROMPT 5
  - Intent: Fix regen detection dropping after first hit; align font size slider ranges
  - Files changed: HealerMana.lua
  - Result: Removed updateUnit() call from UNIT_POWER_FREQUENT handler — mana ticks were racing to clear d.regenLabel by re-running getRegenInfo() on every tick, wiping Drinking/Innervate state set by UNIT_AURA. renderBar reads mana % fresh via readUnitPctRaw, so only refreshDisplay() is needed there. Also changed Mana % Size slider max from 24 to 20 to match Name Size range (8–20).
  - Notes: Regen state (regenLabel/regenIcon) is now exclusively managed by UNIT_AURA and rebuildRoster, not mana ticks.
- PROMPT 6
  - Intent: Remove eating/drinking/Innervate detection — aura queries return nil in M+ and boss encounters (SecretWhenUnitAuraRestricted); no addon workaround possible
  - Files changed: HealerMana.lua
  - Result: Removed REGEN_SPELLS table, regenNames/regenIconById state, buildRegenNames(), getRegenInfo(), regenIcon/regenLabel fields from snapshotUnit/updateUnit, regenLabel render branch in renderBar, UNIT_AURA event handler and registration, /hm buffs slash command. buildSpecIcons() now called directly instead of via buildRegenNames().
  - Notes: Re-add when Blizzard whitelists eating/drinking/Innervate spell IDs as non-secret.
- PROMPT 2
  - Intent: Update Interface version to 120001 across all doc files
  - Files changed: CLAUDE.md, docs/DEV_RULES.md, docs/ADDON_CONTEXT.md
  - Result: All three files now consistently reference 120001; stale TOC mismatch note removed from ADDON_CONTEXT.md
  - Notes: TOC was already 120001; this aligns the docs
- PROMPT 7
  - Intent: Fix dead status stuck on "Dead" after a non-player healer is resurrected (reported in a follower dungeon)
  - Files changed: HealerMana.lua
  - Result: UNIT_FLAGS (which drives d.dead via updateUnit) was registered globally with eventFrame:RegisterEvent, so Blizzard only delivered it for the player's own unit — party/raid healers (including follower NPCs) never got their dead flag re-evaluated on resurrection. Moved UNIT_FLAGS to per-healer eventFrame:RegisterUnitEvent inside rebuildRoster(), matching the existing UNIT_POWER_FREQUENT/UNIT_IN_RANGE_UPDATE pattern (register in the roster loop, unregister at the top of rebuildRoster before re-registering).
  - Notes: Not yet verified in-game (no live repro available this session); please confirm the bar clears to alive state promptly after a revive.
- PROMPT 8
  - Intent: Add a configurable grid layout (rows and columns), alongside the existing horizontal/vertical single-strip layouts
  - Files changed: HealerMana.lua
  - Result: Added DEFAULTS keys gridEnabled (bool, default false) and gridColumns (number, default 4, range 1-8). positionBar and refreshDisplay each gained a grid branch, checked before the existing layoutHorizontal branch, so grid takes priority when enabled but leaves the horizontal/vertical math untouched. Grid uses row-major fill (col = (idx-1) % cols, row = floor((idx-1)/cols)), reusing the existing horizontal step formula (cs + BORDER_OVERHANG + gap) for the x-axis and the existing vertical step formula (cs + nameH + gap) for the y-axis. Frame sizing uses min(count, columns) for width so the draggable frame doesn't reserve dead space when the roster is smaller than the column count. Added "Grid (rows and columns)" toggle and "Grid Columns" slider to the existing Layout config group (placeGroup count updated 1,0 -> 2,1), plus OnShow sync for both new widgets. The existing Horizontal toggle is left as-is and simply ignored while grid is enabled (no cross-widget disable wiring).
  - Notes: Not yet verified in-game. Grid mode inherits the same pre-existing minor vertical border-overhang risk as vertical-strip mode when showName=false and cellSpacing=0 — not new, not fixed, out of scope.
- PROMPT 9
  - Intent: Add a way to test the display (including the new grid layout) without needing to be in a real group
  - Files changed: HealerMana.lua
  - Result: Added /hm raid <n> (1-20, or /hm raid off) which populates healerData with n fake healer entries (generateTestRoster, cycling the six healer classes, last entry flagged dead to spot-check the earlier dead-status fix). Fake entries use unit="player" for the data.unit field specifically so the two live, non-pcall-safe calls in renderBar (isUnitInRange, readUnitPctRaw) always operate on a real, valid unit token instead of a guessed-safe fake one — this also means dimOutOfRange's range check is skipped for fake entries (UnitIsUnit(data.unit,"player") is true) and the mana % shown is your own real live mana. Added testModeActive session flag (not saved) and a one-line guard at the top of rebuildRoster() so real roster-rebuild events (GROUP_ROSTER_UPDATE, PLAYER_ENTERING_WORLD) don't wipe the fake roster while test mode is active; /hm raid off clears the flag and calls the real rebuildRoster() to restore live state. Added help text line for the new command.
  - Notes: Not yet verified in-game.
- PROMPT 10
  - Intent: Redesign the config panel to match the "CharacterStats feel" (tabbed config instead of one long scrolling list) and give sliders a circular thumb like MidnightTracker's, per user request
  - Files changed: HealerMana.lua
  - Result: Researched CharacterStats (UI/ConfigPanel.lua, UI/ConfigWidgets.lua) and MidnightTracker (Config.lua) directly before implementing, per the project's no-guessing-WoW-APIs rule. HealerMana's existing palette was already numerically identical to both reference addons' gold-accent-on-near-black theme, so this was a structural change, not a re-theme. Added a maskCircle(owner, texture) helper (CreateMaskTexture + Blizzard's stock Interface\CHARACTERFRAME\TempPortraitAlphaMask circular mask, ported from MidnightTracker's MaskCircle) and rebuilt makeSlider's thumb as a 14x14 two-layer masked circle (dark ring + inset gold fill) instead of a flat 8x8 square; kept the slider's public API and drag/click interaction logic untouched. Replaced the single scrolling stack of 5 group boxes (Sort Order, Display, Size, Layout, Font Sizes) with a 4-tab layout (General/Display/Layout/Sort, confirmed with user), each tab its own scrollable panel shown/hidden via a new showTab() function; added a createTabButton helper adapted from CharacterStats' tab-button pattern (active tab = brighter background + gold top/side border + hidden bottom border to visually merge with the panel below). placeGroup was parameterized (content/contentY as args instead of closed-over upvalues) so it's reentrant across the 4 panels. Config frame shrunk from 300x520 to 300x380 since content no longer stacks in one long list. Reopening the panel now returns to the last active tab (tracked via f._activeTab) instead of always resetting to General.
  - Notes: Not yet verified in-game. Every existing onToggle/onChange callback body was moved verbatim (not rewritten) into its new tab.
- PROMPT 11
  - Intent: Polish pass on the new tabbed config panel per user feedback — felt cramped, and the content inset was asymmetric (padded on the left, flush against the frame border on the right)
  - Files changed: HealerMana.lua
  - Result: Widened the config frame from 300 to 360 (all other elements are relatively anchored, so this was a single SetSize change). Fixed createTabPanel's scroll frame: its TOPLEFT inherited a 12px inset indirectly via tabBar, but its BOTTOMRIGHT anchored to footer (which spans flush to the frame edges with 0 inset) with no offset — added an explicit -12 x-offset to match, restoring symmetric left/right padding.
  - Notes: Root cause of the inset bug was that only one of the two anchor points (tabBar) carried an inherited inset; the other (footer) needed it stated explicitly since footer itself isn't inset from the frame.
- PROMPT 12
  - Intent: Move the layout-mode selection (horizontal/vertical/grid) out of the Layout tab and into a dropdown on the General tab, with the Layout tab reactively showing only the relevant controls and resizing to fit, per user request
  - Files changed: HealerMana.lua
  - Result: Added a new makeDropdown widget (label + button showing current selection + floating menu of flatBtn rows, closed via a full-screen invisible overlay button) — deliberately smaller than CharacterStats' reference dropdown (which has a dynamic scrollbar for long lists) since this is a fixed 3-item list; reuses the existing flatBtn helper for menu rows rather than inventing new row styling. Removed the "Horizontal (left-to-right)" and "Grid (rows and columns)" toggles from the Layout tab entirely, replaced by a single "Layout Mode" dropdown (Horizontal/Vertical/Grid) on the General tab that writes to the same cfg.layoutHorizontal/cfg.gridEnabled keys — no new SavedVariables. Forward-declared gapSlider/gapLabel/gridColsSlider/layoutG/layoutContent/refreshLayoutPanel before the General tab section (assigned without `local` later in the Layout section) since the dropdown's callback, built first, needs to reach them. Added refreshLayoutPanel() which shows/hides the Grid Columns slider and resizes the Layout tab's group box + scroll content height (2-slider vs 3-slider) based on cfg.gridEnabled; called on every mode change and in OnShow sync so it's always consistent with saved state.
  - Notes: Not yet verified in-game. Rendering logic (positionBar/refreshDisplay's grid-vs-strip branching) is untouched — only the config UI moved.
- PROMPT 13
  - Intent: Auto-size the name/mana% text from Icon Size instead of independent manual sliders, per user request
  - Files changed: HealerMana.lua
  - Result: Added autoFontSizes(cellSize) deriving nameFontSize/pctFontSize via fixed ratios (0.19/0.27, chosen so cellSize=52 reproduces today's defaults 10/14 exactly — zero visual change for anyone at default settings), clamped to the old slider's 8-20 range. Called from two places: the Icon Size slider's onChange (Layout tab), alongside the existing cellSize save and barPool wipe/refresh so new font sizes take effect immediately; and once in ADDON_LOADED right after the DEFAULTS merge, so existing saved profiles with a customized cellSize snap to the correct derived font sizes immediately on this update rather than waiting for the next Icon Size slider touch. Removed the Name Size and Mana % Size sliders entirely from the Display tab (confirmed with user: no manual override toggle) — Display tab is now 4 toggles, 0 sliders. createBar needed no changes since it already reads cfg.nameFontSize/cfg.pctFontSize as plain fields, agnostic to how they're set.
  - Notes: Not yet verified in-game. nameFontSize/pctFontSize remain in DEFAULTS/SavedVariables as derived/cached values rather than being removed, to avoid touching the generic merge loop for no benefit.
- PROMPT 14
  - Intent: Fix long healer names getting cut off — auto-shrink each name label's font to fit its own icon width instead of using one fixed size for every bar
  - Files changed: HealerMana.lua
  - Result: Added MIN_NAME_FONT (6) constant. renderBar's name-text block now, on text change only, sets the font back to cfg.nameFontSize then shrinks it 1pt at a time (SetFont + GetStringWidth loop) until the rendered name fits bar.nameTxt's fixed width (matches the icon width) or hits MIN_NAME_FONT. Cached via bar.nameTxt._lastName so the shrink loop only re-runs when a pool slot's healer actually changes (sort reorder, roster change), not on every 0.5s mana-tick refresh. No extra cache-invalidation needed for cfg.nameFontSize changes — those already come bundled with a full barPool wipe (Icon Size slider), which recreates nameTxt fresh with no _lastName set.
  - Notes: Not yet verified in-game. GetStringWidth/SetFont are standard WoW FontString APIs already implicitly relied on elsewhere in this addon's rendering path.
- PROMPT 15
  - Intent: Fix bug reported by user — the Layout Mode dropdown menu opens but clicking an option doesn't select it
  - Files changed: HealerMana.lua
  - Result: Root cause: child frames don't automatically inherit their parent's frame strata in WoW. makeDropdown's menu row buttons (built via flatBtn, parented to menu) had no explicit strata, defaulting to MEDIUM, while the full-screen click-outside-to-close overlay button was explicitly TOOLTIP strata — so the overlay rendered above the rows and intercepted every click meant for them, closing the menu without ever calling onSelect. Fixed by adding row:SetFrameStrata("TOOLTIP") when each row is created, matching menu's strata so rows render above the overlay (frame level naturally favors the row, since it's nested one level deeper than the overlay).
  - Notes: Also spaced the Layout Mode dropdown 8px further from the Lock frame toggle above it per user feedback (was too close) — generalG/generalContent height bumped +8 to match.
- PROMPT 16
  - Intent: Revert the tabbed config panel back to a single, larger scrolling page organized into titled sections — user decided tabs weren't the right approach after all; also wanted the Layout Mode dropdown above the Lock frame checkbox (was below it)
  - Files changed: HealerMana.lua
  - Result: Removed all tab machinery (createTabButton, tabBar, createTabPanel, showTab, TAB_NAMES/tabButtons/tabPanels, f._activeTab) — fully self-contained within createConfigFrame, nothing else referenced it. Restored the single shared scroll/content pattern from before tabs existed. placeGroup reverted to its pre-tab signature (content closed over again instead of passed as a parameter, since there's only one content column now). Sections restored with titles (General, Display, Sort Order, Layout) since there's no tab label to identify them anymore. Reordered General's contents per user request: Layout Mode dropdown on top, Lock frame checkbox below. Reordered sections so Layout is last (was 3rd) — its height varies with Grid mode (Grid Columns slider shows/hides), and since all sections now share one scrolling column, putting the variable-height section last means resizing it only needs to update the shared content's total height, not reflow anything positioned after it. refreshLayoutPanel updated accordingly: content:SetHeight(layoutTopY + groupH + 4) instead of a separate layoutContent. Frame resized from 360x380 to 360x640 to fit the restacked content without needing to scroll for the common case.
  - Notes: Not yet verified in-game. All widget onToggle/onChange/onSelect callback bodies are unchanged from the tabbed version — this was purely a structural reorganization.
- PROMPT 17
  - Intent: Add a second display style — mana bars instead of the icon grid — selectable via a config dropdown, per user request ("use your imagination")
  - Files changed: HealerMana.lua
  - Result: Verified the key technical risk before implementing, per this project's no-guessing rule: whether a StatusBar fill can be driven by a WoW 12.0 secret-valued mana percentage. Confirmed via Warcraft Wiki (StatusBar:SetValue/SetMinMaxValues are explicitly documented as accepting secret values) and by reading ElvUI's actual installed, live oUF power-bar code (oUF/elements/power.lua:254-264), which passes cur/max completely untouched into SetMinMaxValues/SetValue with zero issecretvalue() guarding, and colors bars only from non-secret data (class/power-type tokens) — confirming class-color fill and passing readUnitPctRaw's result straight into SetValue was both safe and the standard pattern. Added displayStyle ("icon"/"bar"), barWidth (160), barHeight (20) to DEFAULTS. Added autoBarFontSize(barHeight) (ratio 0.55, distinct from icon mode's ratio since bars are short and wide rather than square) and PCT_RESERVE (44px reserved for the %/Dead/DC text on a bar's right edge). Extracted the icon-mode name-shrink loop (added two prompts ago) into a shared shrinkTextToFit(fs, baseSize, maxW) helper, reused by both display styles. createBar/positionBar/renderBar/refreshDisplay each gained a displayStyle=="bar" branch checked first with an early return, leaving all existing icon-mode code paths untouched — bar mode builds a StatusBar-based element (class-colored fill, name left / %-or-Dead-or-DC right) and reuses the exact same horizontal/vertical/grid arrangement math as icon mode (just swapping barWidth/barHeight for cellSize, dropping the outset BORDER_OVERHANG since bar borders are flush/inset instead). Added a Display Style dropdown to the General section (Icons/Bars) — General stays structurally static regardless of style (avoids the reflow-cascade problem from the previous prompt) since the Layout Mode dropdown remains meaningful for bars too. refreshLayoutPanel was generalized from "show/hide one slider" to "compute and reposition whichever slider set is currently relevant" (Icon Size+Spacing[+Grid Columns] vs Bar Width+Bar Height+Spacing), still resizing only the shared content's total height since Layout stays last on the page.
  - Notes: Not yet verified in-game. Grid mode for bars reuses the same (icon-mode) gridColumns value rather than getting its own slider — satisfies "no grid columns [control] for bars" literally without removing grid functionality if a user configures it while in icon mode first.
- PROMPT 18
  - Intent: Fix bug reported by user — selecting "Bars" in the Display Style dropdown changes the actual display but the dropdown button still shows the old label
  - Files changed: HealerMana.lua
  - Result: makeDropdown's row OnClick handler called onSelect(opt.value) (which updates cfg/behavior) but never updated the dropdown's own displayed text — that only happened via external d:SetValue(...) calls at widget creation and in OnShow sync, neither of which re-fires on a click. Added d:SetValue(opt.value) to the row's OnClick, right before onSelect(opt.value), so the widget updates its own label immediately on selection regardless of what the caller's onSelect does. Same fix applies to both dropdowns in the file (Display Style, Layout Mode) since they share the one makeDropdown widget.
  - Notes: Not yet verified in-game.
- PROMPT 19
  - Intent: Fix bug reported by user — on the Layout section, each slider's label reads as though it belongs to the slider above it, not its own
  - Files changed: HealerMana.lua
  - Result: In makeSlider, the label was anchored to the slider frame's TOPLEFT (y=0) while the track was anchored to the frame's BOTTOMLEFT (only 4px up from the 40px-tall frame's bottom) — leaving a ~36px gap between a slider's own label and its own track, but only an ~11px gap between that track and the NEXT slider's label 44px below (since sliders stack with only a 4px margin between frames). The track visually read as belonging to the row underneath it. Fixed by anchoring the track to the frame's TOPLEFT/TOPRIGHT with a fixed -20 y-offset instead of bottom-anchoring it — now the label-to-track gap is a constant ~20px for every slider, and the leftover space sits at the bottom of each row (neutral inter-row padding) instead of being sandwiched confusingly between one slider's track and the next slider's label.
  - Notes: Not yet verified in-game. hit/redraw/thumb positioning all read track's resolved geometry via GetWidth/GetLeft/GetRight, not the anchor point used, so no other code needed touching.
- PROMPT 20
  - Intent: Add "Hide own bar" and per-instance-type visibility filtering, gaps surfaced by comparing HealerMana against CurseForge competitors (HealerManaBars, Raid Healer Mana Bars) at the user's request
  - Files changed: HealerMana.lua
  - Result: Added hideSelf and six showInX (OpenWorld/Dungeons/Raids/Scenarios/Battlegrounds/Arenas) keys to DEFAULTS, all defaulting to today's behavior (hideSelf=false, every showInX=true). hideSelf is filtered inside rebuildRoster only (not getSorted/renderBar/generateTestRoster) — deliberately, since /hm raid <n> test-mode entries also use unit="player" internally as an unrelated technical trick (PROMPT 9) and must never be affected by this toggle. Raid branch guards with UnitIsUnit(unit,"player") since your own raid index isn't fixed like party's "player" token is. Toggling hideSelf calls rebuildRoster()+refreshDisplay() (roster membership actually changes), unlike the showInX toggles which only call refreshDisplay() (they just gate mainFrame's visibility). Added shouldShowForCurrentInstance(), reading IsInInstance()'s instanceType (zone metadata, not combat state — unaffected by WoW 12.0 secret values, confirmed not to need the same verification pass as the StatusBar work) and mapping it to the matching showInX default (unrecognized types default to showing, not hiding). Wired into refreshDisplay's two mainFrame:Show()/Hide() decision points (bar-mode and icon-mode branches). No new event registration needed — PLAYER_ENTERING_WORLD already rebuilds+refreshes on every zone transition. UI: "Hide own bar" added to Display (5th toggle); six visibility checkboxes added to General between Layout Mode and Lock frame (General now 7 toggles + 2 dropdown-slot rows), per user's choice to fold into existing sections rather than a new one.
  - Notes: Not yet verified in-game.
- PROMPT 21
  - Intent: Fix bug reported by user — bars have a border shape but it never reflects the "Class color border" toggle the way icon mode's border does
  - Files changed: HealerMana.lua
  - Result: createBar's bar-mode branch already builds bar.border (flush BackdropTemplate, UI-Tooltip-Border) same as icon mode does, but renderBar's bar-mode branch never called SetBackdropBorderColor on it — it stayed a static grey set once at creation, never wired to cfg.borderClassColor unlike the icon-mode branch below it. Rather than removing the toggle for bar mode (the other option the user offered), wired it up the same way icon mode already does: right after computing cc = CLASS_COLOR[data.class], bar.border now gets class-colored when cc and cfg.borderClassColor are both true, else the same neutral 0.85 grey — applied before the dead/DC/live branch so it's set unconditionally on every render, matching icon mode's placement.
  - Notes: Not yet verified in-game.
- PROMPT 22
  - Intent: Add a bar texture picker sourced from LibSharedMedia-3.0 (and, by ecosystem convention, effectively Details! and anything else that registers into it), if available, per user request
  - Files changed: HealerMana.lua
  - Result: Verified before implementing (no live client to test against, per this project's no-guessing rule): read LibSharedMedia-3.0's actual installed source (it's embedded by dozens of already-installed addons plus a standalone copy) to confirm the real API rather than guess — LibStub:GetLibrary("LibSharedMedia-3.0", true) for a soft/optional lookup, LSM:List("statusbar") for a sorted name array, LSM:Fetch("statusbar", key) for the texture path, and confirmed 4 built-in entries always exist (Blizzard, Blizzard Character Skills Bar, Blizzard Raid Bar, Solid) even with nothing else registered. Details! isn't installed locally so its specific API couldn't be verified the same way, but it (like virtually everything else) registers into LSM as the standard interop point, so supporting LSM alone covers it. This is a soft runtime dependency, not an embedded library — HealerMana ships nothing extra and falls back to today's hardcoded texture with zero behavior change if no addon provides LSM. Added getLSM() helper (checks _G.LibStub), barTexture DEFAULTS key (stores the LSM key name, not a resolved path), and wired createBar's bar-mode branch to resolve+apply it. Config UI: a 4th reactive Layout-section row (Bar Texture dropdown, bar mode only), built only if LSM is present at first-config-open time — options list captured once then, reasoned safe since by the time a user opens /hm every other addon has already loaded and registered its media.
  - Notes: Not yet verified in-game. No .toc changes — OptionalDeps ordering is irrelevant since the LSM lookup only happens lazily on first /hm open, well after all addons have loaded.
- PROMPT 23
  - Intent: Add an option to color the player name by class color or leave it white, per user request
  - Files changed: HealerMana.lua
  - Result: Added nameClassColor DEFAULTS key (true, preserving today's always-class-colored behavior). Both renderBar branches' name-color logic changed from "if cc then class-color else neutral-grey" to "if cc and cfg.nameClassColor then class-color else white (1,1,1)" — merges the old no-class-data fallback and the new off-state into one white color rather than keeping a separate shade for each. Bar mode's block was split: bar.fill's class-coloring is untouched (still always class-colored when cc exists, governed only by the existing borderClassColor-independent fill logic), only nameTxt's color now additionally checks the new toggle — scoped exactly to "player name" per the request, not fill or border. Dead/DC states keep their own fixed grey regardless of this toggle (that's a state indicator, not a class-color choice). Added "Class color name" toggle to Display (6th toggle now), placed directly under the existing "Class color border" toggle.
  - Notes: Not yet verified in-game.
- PROMPT 24
  - Intent: Add a way to handle long names other than auto-shrinking (which can make text unreadably small), per user request
  - Files changed: HealerMana.lua
  - Result: Added nameOverflow DEFAULTS key ("shrink" default, preserving today's behavior; "truncate" as the new alternative). Added stripLastUTF8Char (removes one full UTF-8 character from the end without ever splitting a multi-byte sequence — WoW's Lua 5.1 sandbox has no built-in utf8 library) and truncateTextToFit (keeps a fixed font size, strips one character at a time from the end with a trailing "…" until the string fits maxW). Both renderBar name-text blocks (icon and bar mode) now branch on cfg.nameOverflow: "truncate" sets the font once to the fixed base size then calls truncateTextToFit; anything else keeps calling the existing shrinkTextToFit unchanged. Added "Long Name Handling" dropdown (Shrink to Fit / Truncate) to Display, after "Hide own bar". Its onSelect clears _lastName on every pooled bar's nameTxt (not a full barPool wipe — nameOverflow doesn't change whether nameTxt exists, just how it renders, so a full recreate isn't needed) so the new mode takes effect immediately on the next refreshDisplay() rather than waiting for a name to actually change.
  - Notes: Not yet verified in-game. Truncation is character-safe for non-ASCII names (Cyrillic/Korean/etc. realms) via the UTF-8-aware stripping helper.
- PROMPT 25
  - Intent: Fix bug reported by user — the mana % text looked bolder/different outline than the name text
  - Files changed: HealerMana.lua
  - Result: pctTxt was created with "THICKOUTLINE" in both createBar branches (icon mode and bar mode) while nameTxt consistently used "OUTLINE" everywhere, including inside shrinkTextToFit/truncateTextToFit — since pctTxt's font is set once at creation and never touched again by those helpers, it stayed visually heavier than the name permanently, regardless of nameOverflow mode. Changed both pctTxt:SetFont calls from THICKOUTLINE to OUTLINE, matching nameTxt exactly. No THICKOUTLINE references remain anywhere in the file, so this stays consistent automatically even as the shrink/truncate helpers keep resetting nameTxt's font — there's nothing left that could drift out of sync.
  - Notes: Not yet verified in-game.
- PROMPT 26
  - Intent: Widen the config panel into two columns so every option is visible at once without scrolling, per user request
  - Files changed: HealerMana.lua
  - Result: placeGroup regained a leading parent parameter (content -> leftColumn/rightColumn) so groups can anchor into either column instead of one shared content frame. Added two fixed-width (320px, 24px gap) column frames inside content — fixed constants rather than dynamic fractions, since the config frame isn't user-resizable. Computed each section's current height from the existing innerH/groupH formula and picked the best-balanced pairing: left = General + Sort Order (~386px, both static), right = Display + Layout (up to ~444px, Layout still reactive to display style/grid mode). Threaded two independent cursor variables (leftY, rightY) through the 4 placeGroup calls instead of one shared contentY. Captured leftFinalH once after Sort Order (left column never changes height) and updated refreshLayoutPanel's content:SetHeight to math.max(leftFinalH, layoutTopY + groupH + 4) so the shared scroll content always sizes to whichever column is currently taller. Frame resized from 360x640 to 700x560 (two 320px columns + 24px gap + existing 24px scroll margin = 700 wide; sized to the worst-case right-column height with slack so scrolling isn't needed for today's content — the mousewheel handler stays as a fallback). No section's internal construction (toggles/dropdowns/sliders/callbacks) changed at all — purely a repositioning/re-parenting change.
  - Notes: Not yet verified in-game.
- PROMPT 27
  - Intent: Fix bug reported by user right after PROMPT 26 shipped — config panel opened with title bar + footer visible but a completely blank content area (no sections at all)
  - Files changed: HealerMana.lua
  - Result: leftColumn/rightColumn (added in PROMPT 26) only ever had SetWidth called on them, never SetHeight — unlike content itself, which always eventually gets an explicit height via refreshLayoutPanel. That's a real asymmetry: content was "fixed" by construction, the two new column frames never were, leaving them zero-height. Changed both to SetSize(COLUMN_W, 600) (a generous fixed height, since these are just layout containers — nothing visually depends on their exact height, only their children's own explicit positions/sizes and content's separately-computed height matter for appearance).
  - Notes: Could not reproduce/verify live (no game client access this session) — applied based on static code review identifying the leading structural suspect. Asked the user to confirm the exact symptom first (title+footer only, blank content) before fixing, to avoid guessing blind. If this doesn't fully resolve it, next step is checking a Lua error log (BugSack is already installed) for an exact line number rather than continuing to guess.
- PROMPT 28
  - Intent: Fix bug reported by user — inset spacing around the two-column layout isn't the same all the way around
  - Files changed: HealerMana.lua
  - Result: Traced the actual pixel math: content width = frame(700) - scroll's 12px side margins = 676. The two columns (320 each) + COLUMN_GAP(24) only totaled 664 — since nothing anchored rightColumn's own right edge to content's right edge, the leftover 12px of content width became unaccounted slack past rightColumn, making the right-side gutter (12 scroll margin + 12 slack = 24) effectively double the left-side gutter (12 scroll margin only). Separately, scroll's vertical margins (8px) never matched its horizontal margins (12px) at all. Fixed both: scroll's TOPLEFT/BOTTOMRIGHT y-offsets changed from -8/+8 to -12/+12 (now 12px on all four outer sides), and COLUMN_W/COLUMN_GAP changed from 320/24 to 332/12 so the two columns exactly fill content's width (2*332+12=676, zero slack) with COLUMN_GAP matching the same 12px used everywhere else — every horizontal gutter (frame edge -> left column -> gap -> right column -> frame edge) and the outer vertical margins are now uniformly 12px.
  - Notes: Not yet verified in-game. Each group box's internal top inset (22px, vs 8px on the other three sides) is unchanged and not a bug — that's reserved space for the section's title header text, a functional necessity rather than an arbitrary inconsistency.
- PROMPT 29
  - Intent: Full cleanup pass per user request — strip all comments, remove dead/redundant code, deduplicate repeated logic, refactor for structure. No functional/behavior changes intended.
  - Files changed: HealerMana.lua
  - Result: Removed all comments (file header, section-divider banners, inline explanations) per this project's no-comments rule. Removed dead code: unused BAR_HEIGHT constant, a duplicated debug print line in `/hm range`, and the non-functional `sortMana`/`/hm mana` feature (getSorted() never read cfg.sortMana — mana-sort was already disabled for 12.0 secret values and silently fell through to alpha sort; not listed in `/hm help` either). Deduplicated repeated logic into 2 new helpers: rebuildBarPool() (the `for _,b in ipairs(barPool) do b:Hide() end; wipe(barPool)` sequence, previously repeated 7x across the config UI) and resetPosition() (position-reset logic, previously duplicated between the footer button and `/hm reset`). Slash-command toggles (lock/alpha/class/layout) now reuse the existing saveKey() helper instead of hand-rolling cfg/HealerManaDB writes. Structurally hoisted the 7 UI-widget factories (addBorder, maskCircle, flatBtn, makeToggle, makeSlider, makeGroup, makeDropdown) plus the config color palette out of createConfigFrame() to module scope — createConfigFrame() only ever runs once so this wasn't a perf fix, purely readability/structure (createConfigFrame shrank from ~750 lines to just its own layout/composition code). File went from 2019 to 1733 lines. Verified with luacheck after each pass (no syntax errors introduced) and confirmed every hoisted/new helper is defined exactly once.
  - Notes: Not yet verified in-game — this was a structural/cosmetic-code cleanup with no intended behavior change, but every render/config/slash code path was touched at least once during comment removal, so a full in-game smoke test (open /hm, toggle each option, switch display styles, test /hm raid, /hm reset) is recommended before treating this as done.
